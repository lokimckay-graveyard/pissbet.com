import fontawesome from "../lib/fontawesome";
import "../styles/globals.scss";

fontawesome();

const PissBet = ({ Component, pageProps }) => {
  return <Component {...pageProps} />;
};

export default PissBet;
